# Authoring principles

Prose about the wiki rather than part of it. It carries no frontmatter and
nothing links to it, so a validator that treated it as a page would report an
orphan on the one file explaining what an orphan is.
