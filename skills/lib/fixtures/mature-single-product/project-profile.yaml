# A mature commerce monorepo: single product, ships continuously.
# No roadmap, because there is no discrete release event to sequence toward.

wiki:
  root: docs/wiki
  profiles: [business, technical]
  # Ships alone inside a separate assistant service, so it may not link
  # outward.
  business_subtree: business
  # A deliberate parenthetical citation convention across many pages.
  path_citations: citation

tracker:
  backend: clickup

taskflow:
  enabled: true

house_rules: docs/house-rules.md

generated_paths:
  - "hasura/**/*.yaml"

docs:
  root: docs
  lifecycle: ["specs/*.md"]
  live: ["README.md"]
  reference: ["handover/**", "partners/**"]
  ignored: ["superpowers/**"]

mode:
  default: mature
